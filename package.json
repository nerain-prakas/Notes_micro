{
  "name": "angularjs-notes-micro-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx http-server -c-1 -p 5173 .",
    "build": "rm -rf dist && mkdir -p dist/styles dist/components dist/controllers dist/services && cp index.html app.module.js app.routes.js dist && cp -r styles/* dist/styles && cp -r components/* dist/components && cp -r controllers/* dist/controllers && cp -r services/* dist/services",
    "format": "npx prettier --write \"**/*.{js,css,html,json,md}\""
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.3.3"
  }
}
